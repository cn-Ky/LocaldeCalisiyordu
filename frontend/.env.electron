# Masaüstü (Electron) derlemesi için bu dosya, .env içindeki uzak
# VITE_API_URL değerini (Render/Vercel için kullanılan) geçersiz kılar.
# Gömülü backend'in adresi zaten çalışma anında src/api.js tarafından
# otomatik olarak çözülür (bkz. resolveBaseUrl()).
VITE_API_URL=
